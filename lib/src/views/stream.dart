import 'package:flutter/material.dart';

class TwitarrStream extends StatelessWidget {
  const TwitarrStream({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
